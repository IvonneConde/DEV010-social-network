/**
 * @jest-environment jsdom
 */
// eslint-disable-next-line import/no-extraneous-dependencies
describe('Suite de pruebas para StartPage', () => {
  test('Mi primera prueba', () => {
    // Aquí colocarías tu código de prueba
    // Por ejemplo: expect(2 + 2).toBe(4);
  });
});
